<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PlatformTarget>x64</PlatformTarget>
    <Platforms>x64</Platforms>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
  </PropertyGroup>

  <ItemGroup>
    <!-- DLL CtApi será copiada para o diretório de saída -->
    <Content Include="libs\CtApi\*.dll" Condition="Exists('libs\CtApi\CtApi.dll')">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
  </ItemGroup>

  <ItemGroup>
    <!-- Excluir arquivos da pasta citect-main da compilação -->
    <Compile Remove="citect-main\**" />
    <EmbeddedResource Remove="citect-main\**" />
    <None Remove="citect-main\**" />
  </ItemGroup>

</Project>

